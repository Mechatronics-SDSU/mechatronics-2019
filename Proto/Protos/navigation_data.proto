syntax = "proto3";

message NAV_DATA {
    //AHRS DATA
    float roll  = 1;
    float pitch = 2;
    float yaw   = 3;

    //From pressure Transducers
    float depth = 4;

    //X and Y translation from the DVL
    float north_pos = 5;
    float east_pos = 6;

}
